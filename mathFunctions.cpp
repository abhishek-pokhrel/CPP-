#include <iostream>
#include <cmath>

int main(){

    double x = 3;
    double y = 4;
    double z;

    // z = std::max(x, y); returns 4 (maximum value among 2 variables)
    // z = std::min(x, y); // returns 3 (minimum value among 2 variables)

    // z = pow(2, 3); // returns 8
    // z = sqrt(9); // returns 3
    // z = abs(-3); // returns 3
    // z = round(3.14); // returns 3
    // z = ceil(3.14); // returns 4
    // z = floor(3.99); // returns 3

    std::cout << z; // returns 9

    return 0;
}